{
	"name": "template",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"typegen": "wrangler types",
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"build:wasm": "cd wasm && GOOS=js GOARCH=wasm go build -o ../src/main.wasm *.go"
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.3",
		"@types/golang-wasm-exec": "^1.15.2",
		"typescript": "^5.5.2",
		"wrangler": "^4.27.0"
	},
	"dependencies": {
		"hono": "^4.8.10"
	}
}
